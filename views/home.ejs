<%- include('partials/header') -%>
<h1>Home</h1>
<p><%= startingContent %></p>

<% if (posts.length !== 0) { %>
  <% posts.forEach((post) => { %>

  <h2><a href='/posts/<%= post.title %>'> <%= post.title %> </a></h2>
  <p> <%= post.body.substr(0, 100).concat('...') %> </p>

  <% }); %>
<% } %>

<%- include('partials/footer') -%>
